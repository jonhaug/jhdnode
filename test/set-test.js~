var set = require('../lib/set');

exports['set'] = function (test) {
    var set = new set.Set([1,2,3,4,5]);
    test.equal(set.has(3), true);
    //    test.throws(function () { doubled.calculate('123'); });
    test.done();
};
